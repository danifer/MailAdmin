{% extends 'base.html.twig' %}

{% block title %}MailAlias{% endblock %}

{% block body %}
    <h1>MailAlias</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ mail_alias.id }}</td>
            </tr>
            <tr>
                <th>Source</th>
                <td>{{ mail_alias.source }}</td>
            </tr>
            <tr>
                <th>Destination</th>
                <td>{{ mail_alias.destination }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_mail_alias_index') }}">back to list</a>

    <a href="{{ path('app_mail_alias_edit', {'id': mail_alias.id}) }}">edit</a>

    {{ include('mail_alias/_delete_form.html.twig') }}
{% endblock %}
