{% extends 'base.html.twig' %}

{% block title %}MailAlias{% endblock %}

{% block body %}
    <h1>MailAlias</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ mail_alias.id }}</td>
            </tr>
            <tr>
                <th>Source</th>
                <td>{{ mail_alias.source }}</td>
            </tr>
            <tr>
                <th>Destination</th>
                <td>{{ mail_alias.destination }}</td>
            </tr>
        </tbody>
    </table>

    <div class="mt-4">
        <a href="{{ path('app_mail_alias_index') }}" class="btn btn-secondary">Back to List</a>
        <a href="{{ path('app_mail_alias_edit', {'id': mail_alias.id}) }}" class="btn btn-primary">Edit</a>
    </div>

    {{ include('mail_alias/_delete_form.html.twig') }}
{% endblock %}
