<form method="post" action="{{ path('app_mail_alias_delete', {'id': mail_alias.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');" class="mt-3">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ mail_alias.id) }}">
    <button class="btn btn-danger">Delete</button>
</form>
