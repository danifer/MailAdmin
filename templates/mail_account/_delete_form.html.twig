<form method="post" action="{{ path('app_mail_account_delete', {'id': mail_account.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');" class="mt-3">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ mail_account.id) }}">
    <button class="btn btn-danger">Delete</button>
</form>
